#include "Day04.h"

#include <vector>

#include "../Utility/Utilitiy.h"

using namespace Day04;

std::string Day04::firstPart() {
    auto input = Utility::readInput(inputPath);
    int result = 0;

    return std::to_string(result);
}

std::string Day04::secondPart() {
    auto input = Utility::readInput(inputPath);
    int result = 0;

    return std::to_string(result);
}
