#include "Day05.h"

#include <vector>

#include "../Utility/Utilitiy.h"

using namespace Day05;

std::string Day05::firstPart() {
    auto input = Utility::readInput(inputPath);
    int result = 0;

    return std::to_string(result);
}

std::string Day05::secondPart() {
    auto input = Utility::readInput(inputPath);
    int result = 0;

    return std::to_string(result);
}
